import React from 'react';

const Footer = ({ T }) => (
    <footer className="footer">
        <p className="footer-text">
            {T('footer_text')}
        </p>
    </footer>
);

export default Footer;